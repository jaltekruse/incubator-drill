/**
 * Internal configuration state definitions.
 *
 * Overall structure allows for configurations to be nested within one another to provide
 * hierarchical organization of configuration options.
 *
 */
package org.apache.drill.common.config;
